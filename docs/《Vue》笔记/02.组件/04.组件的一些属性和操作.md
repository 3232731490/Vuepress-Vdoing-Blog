---
title: 组件的一些属性和操作
date: 2021-09-27 06:47:44
permalink: /pages/10ced2/
categories:
  - 《Vue》笔记
  - 组件
tags:
  - Vue
author:
  name: nilu
  link: https://github.com/3232731490
---

## ref属性

```java
    1、被用来给元素或子组件注册引用信息（替代id）
    2、应用在html上获取的是真实Dom元素，应用在组件上是组件实例对象
    打标识：<h2 ref='xxx'>...</h2>
    获取： this.$refs.xxx
```

## 配置项props

```java
      功能： 让组件接受外部传过来的数据
      (1) 传递数据：
            <Demo name='XX'></Demo>
      (2) 接收数据
            ①（只接收）
            props['name']
            ② (限制类型)
            props:{
                  name:String
            }
            ③ (限制类型，限制必要性，指定默认值)
            props:{
                  name:{
                        type:String,
                        required:true,
                        default:'NiLu'
                  }
            }
      备注：props是只读的，Vue底层会检测你对props的修改，如果进行修改会发出警报
            实际开发中若需要修改则复制一份数据，对这个复制的数据进行修改操作,如在data中申明myname:this.name

```

## mixin(混入)

```java
      功能：可以把多个组件公共的配置提取成一个混合对象

      使用方式：
            第一步定义混合，例如：
            {
                  data(){....},
                  methods(){....}
                  ....
            }
            第二部使用混入，例如
            (1) 全局混入：Vue。mixins(xx)
            (2) 局部混入：mixins:[xxx]

```

## 插件

```java
      功能：用于增强Vue
      本质：包含install方法的一个对象，install的第一个参数是Vue，第二个以后的参数是插件使用者传递到数据

      定义插件：
            对象.install=function(Vue,options){
                  //1、添加全局过滤器
                  Vue.filter(...)

                  // 2、添加全局指令
                  Vue.directive(...)

                  // 3、配置全局混入
                  Vue.mixin(...)

                  // 4、添加实例方法
                  Vue.prototype.$myMethod = function(){...}
                  Vue.protorype.$myProperty=xxx
            }
      使用插件：Vue.use(..)
```

## scoped样式

```java
      作用：让央视在局部生效，防止冲突
      写法：<style scoped>
```

## Tips
```java
      1、组件化编码流程：
            (1) 拆分静态组件：组件要按照功能点拆分，命名不要与html元素冲突
            (2) 实现动态组件：考虑好数据的存放位置，数据是一个组件再用还是多个组件再用
                  1）多个组件再用，放在他们的共同父组件上（状态提升）
                  2）一个组件再用：放在自身上即可
            (3) 实现交互：从绑定事件开始
      
      2、props适用于
            (1) 父组件 ==> 子组件通信
            (2) 子组件 ==> 父组件通信（要求先给子一个函数）

      3、 使用v-model切记：**v-model绑定的值不能是props传过来的值**（props不可修改）

      4、props传过来的若是对象类型的值，修改对象中的属性不会报错，但不推荐
```
